import './estilos.css'

const ItemListContainer = (props) => {
    return(
        <>
        <h2 className="Bienvenida">Hola {props.nombre} bienvenido a la mejor e-commerce del mundo, que actualmente no tiene nada en ella</h2>
        </>
    )
}

export default ItemListContainer